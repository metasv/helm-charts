apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fluentd-elasticsearch.fullname" . }}-test-connection"
  labels:
    {{- include "fluentd-elasticsearch.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fluentd-elasticsearch.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
